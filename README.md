# Rotation-angle signatures

Research prototype for studying how fixed rotation angles affect self-supervised visual
representations across datasets and encoder architectures.

The initial study will establish deterministic experiment configuration, fixed-angle data
views, contrastive pretraining, frozen downstream probes, and reproducible result capture.
